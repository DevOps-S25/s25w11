name: wf-ci
on:
  pull_request:
    types:
      - opened
      - synchronize
    branches:
      - develop
  push:
    branches:
      - develop
jobs:
  ci-job:
    runs-on: ubuntu-latest
    steps:
      - name: Welcome step
        run: echo "Welcome To the GitHub Actions"
      - name: Clone Repo
        uses: actions/checkout@v4
      - name: Repo Information
        run: |
          
          echo "Event Name: ${{github.event_name}}"
          echo "Actor Name: ${{github.actor}}"
          echo "Brranch Name: ${{github.ref_name}}"
        